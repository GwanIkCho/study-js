// const userLayout = (() => {
//     const showList = (users) => {
//         const table = document.getElementById("users");
//         let text = "";
//         users = users.slice(0, 2);
//         users.forEach((user) => {
//             text += `<tr><td>${user.name}</td><td>${user.email}</td></tr>`;
//         });
//         table.innerHTML = text;
//     };
//     return { showList: showList };
// })();


const postLayout